ROOM
roomId:string
playerList
json:
	name:string
	minDistance:20
	height:number
	width: number
	margin:number
	roomId:tavern
	tileMap:Array(Array(number)[8])
	tilesize: number
	artwork:
		background
		forground
		props:
			Array(Array(number)[3])
		sprites:
			frames: Array(Array(number)[7])
			images: Array(string)


this is probably outdated but there was the comment and it seems important

needs to be sorted out